#!/usr/bin/env python3
import signal

import rpc
import webui


def start():
    rpc_server = rpc.serve()
    webui_server = webui.serve()
    try:
        while True:
            signal.pause()
    except KeyboardInterrupt:
        pass


if __name__ == '__main__':
    start()
